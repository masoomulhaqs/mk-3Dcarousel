(function ( $ ) {
 
    
	$.fn.mkCarousel = function(settings){
		var $target = $(this.selector+'>img'), leftMargin;
		var defaults = $.extend({
			offset: 100,
			mobileOffset: 40
		}, settings);
		this.slideIt = function(){
		}
	}

 
}( jQuery ));